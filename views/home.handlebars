<section class="container">
  <h1>OmTechBlog Post</h1>
  {{#each posts as |post|}}
  <div class="form">
    <a href='/posts/{{id}}'><h2>
      {{title}}
    </h2></a>
    <div class="form-body">
      {{body}}
    </div>
  </div>
  {{/each}}
</section>